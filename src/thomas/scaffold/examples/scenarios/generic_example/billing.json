{
  "schema_version": "1.0",
  "feature": "billing",
  "description": "Test billing charge creation",
  "request": {
    "method": "POST",
    "path": "/charges",
    "body": {
      "customer_id": "{{customer_name}}",
      "amount": "{{transfer_amount}}"
    }
  },
  "api_checks": [
    {
      "path": "status_code",
      "operator": "equals",
      "expected": 200
    },
    {
      "path": "body.status",
      "operator": "equals",
      "expected": "completed"
    }
  ],
  "validations": []
}
